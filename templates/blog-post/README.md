# Blog Post Draft Templates

This folder contains hidden authoring templates and drafts.

- `_skeleton/` defines the required structure for every new post draft.
- `drafts/<slug>/` is generated by `npm run blog:new -- --slug <slug> --title "..."`.

Files in `templates/` are not routed by Astro and will not be shown on the website.
